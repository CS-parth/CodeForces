class Solution {
public:
    int dp[100001];
    long long minIncrementOperations(vector<int>& nums, int k) {
        // memset(dp,-1,sizeof(dp));
        for(int i = 0; i < nums.size()-1;i++){
            // make it greater than k 
            if(int i 
                
            }
        }
    }
};